package src;

public class CreateResourceController {

    private boolean createResource(){

        //This is the main function that will handle adding new resources.

        return true;
    }

    private boolean checkResource(){

        //This is extra functionality but don't think we should all resources with the same name on the system as more copies could be made instead.

        return true;
    }

}
